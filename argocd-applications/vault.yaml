apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: vault
  namespace: gitops
spec:
  project: default
  source:
    repoURL: "https://github.com/usmanali/platform-gitops.git"
    targetRevision: HEAD
    path: apps/vault
  destination:
    server: "https://kubernetes.default.svc"
    namespace: platform
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
